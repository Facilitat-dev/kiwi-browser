# v1 APK cut

## In

- Name **Canopy**, package `app.canopy.browser`
- Folio Calm Sage palette and launcher mipmaps from `canopy-app-icon.svg`
- Chromium **153.0.8010.39** with `is_desktop_android = true`
- Afterbird extension patches (MV2, Web Store desktop UA, phone extensions menu, unpacked load)
- Privacy GN flags in `.build/args/release.gn` (no Google keys, trials, reporting, remoting, Widevine)
- Folio Calm Sage tokens in `branding/android/.../colors.xml`
- uBlock Origin **1.62.0** packed in the APK as `assets/ublock.zip`, extracted on first launch, loaded unpacked
- Dev signing keystore at `~/.config/facilitat-browser/dev.keystore` (not in git)

## Out (backlog)

- Kiwi bottom bar, night mode, classic NTP, edge-swipe extras
- Cromite/ungoogled source patches (need the 153 tree + license review)
- Shipping the Relixor name or icons
- Play Store / public release signing

## Done when

`chrome_public_apk` installs, opens with uBlock Origin already loaded, and does not call `*.kiwibrowser.com`.
