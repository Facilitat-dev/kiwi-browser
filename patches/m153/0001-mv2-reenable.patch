diff --git i/extensions/browser/manifest_v2_util.cc w/extensions/browser/manifest_v2_util.cc
index 08d7786b3b..4a09e8e85d 100644
--- i/extensions/browser/manifest_v2_util.cc
+++ w/extensions/browser/manifest_v2_util.cc
@@ -20,6 +20,8 @@ bool IsExtensionAffected(const Extension& extension) {
 bool IsExtensionAffected(int manifest_version,
                          Manifest::Type manifest_type,
                          mojom::ManifestLocation manifest_location) {
+  // Canopy: MV2 re-enabled — never treat any extension as deprecated.
+  return false;
   // Only extensions < MV3.
   if (manifest_version >= 3) {
     return false;
