diff --git i/chrome/android/java/src/org/chromium/chrome/browser/ChromeTabbedActivity.java w/chrome/android/java/src/org/chromium/chrome/browser/ChromeTabbedActivity.java
index 888a4909ba..00b8306278 100644
--- i/chrome/android/java/src/org/chromium/chrome/browser/ChromeTabbedActivity.java
+++ w/chrome/android/java/src/org/chromium/chrome/browser/ChromeTabbedActivity.java
@@ -4503,7 +4503,21 @@ public class ChromeTabbedActivity extends ChromeActivity implements PreAttachInt
         } else if (id == R.id.extensions_menu_menu_id) {
             ExtensionsToolbarCoordinator coordinator =
                     getToolbarManager().getExtensionsToolbarCoordinator();
-            coordinator.showExtensionsMenu();
+            if (coordinator != null) {
+                coordinator.showExtensionsMenu();
+            } else {
+                // Phone form factor: toolbar extensions coordinator is tablet-only.
+                LoadUrlParams extParams =
+                        new LoadUrlParams(
+                                UrlConstants.CHROME_EXTENSIONS_URL, PageTransition.AUTO_TOPLEVEL);
+                if (currentTab == null) {
+                    getTabCreator(getCurrentTabModel().isIncognito())
+                            .createNewTab(
+                                    extParams, TabLaunchType.FROM_CHROME_UI, /* parent= */ null);
+                } else {
+                    currentTab.loadUrl(extParams);
+                }
+            }
             RecordUserAction.record("MobileMenuExtensionsMenu");
         } else if (id == R.id.manage_extensions_menu_id) {
             LoadUrlParams params =
