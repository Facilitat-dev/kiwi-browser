diff --git i/chrome/browser/content_settings/request_desktop_site_web_contents_observer_android.cc w/chrome/browser/content_settings/request_desktop_site_web_contents_observer_android.cc
index bb030c4e47..87745f65cf 100644
--- i/chrome/browser/content_settings/request_desktop_site_web_contents_observer_android.cc
+++ w/chrome/browser/content_settings/request_desktop_site_web_contents_observer_android.cc
@@ -71,6 +71,11 @@ void RequestDesktopSiteWebContentsObserverAndroid::DidStartNavigation(
   ContentSetting setting = host_content_settings_map_->GetContentSetting(
       url, url, ContentSettingsType::REQUEST_DESKTOP_SITE, &setting_info);
   bool desktop_mode = setting == CONTENT_SETTING_ALLOW;
+  // Canopy: Chrome Web Store only shows Install to desktop user agents.
+  const bool is_web_store =
+      url.host() == "chromewebstore.google.com" ||
+      (url.host() == "chrome.google.com" && url.path().starts_with("/webstore"));
+  desktop_mode |= is_web_store;
   // For --request-desktop-sites, always override the user agent.
   bool always_request_desktop_site =
       base::CommandLine::ForCurrentProcess()->HasSwitch(
@@ -98,7 +103,7 @@ void RequestDesktopSiteWebContentsObserverAndroid::DidStartNavigation(
   // Override UA for renderer initiated navigation only. UA override for browser
   // initiated navigation is handled on Java side. This is to workaround known
   // issues crbug.com/40801643 and crbug.com/40799177.
-  if (navigation_handle->IsRendererInitiated()) {
+  if (navigation_handle->IsRendererInitiated() || is_web_store) {
     navigation_handle->SetIsOverridingUserAgent(desktop_mode);
   }
 
diff --git i/chrome/browser/ui/android/desktop_site/java/src/org/chromium/chrome/browser/desktop_site/DesktopSiteUtils.java w/chrome/browser/ui/android/desktop_site/java/src/org/chromium/chrome/browser/desktop_site/DesktopSiteUtils.java
index 028311d52e..f062a58b99 100644
--- i/chrome/browser/ui/android/desktop_site/java/src/org/chromium/chrome/browser/desktop_site/DesktopSiteUtils.java
+++ w/chrome/browser/ui/android/desktop_site/java/src/org/chromium/chrome/browser/desktop_site/DesktopSiteUtils.java
@@ -221,6 +221,10 @@ public class DesktopSiteUtils {
             return true;
         }
 
+        if (isChromeWebStore(url)) {
+            return true;
+        }
+
         // If domain/global setting is enabled and window setting should not
         // apply, override the user agent.
         if (readRequestDesktopSiteContentSettings(profile, url)
@@ -236,6 +240,18 @@ public class DesktopSiteUtils {
         return false;
     }
 
+    /** Whether {@code url} is a Chrome Web Store page. */
+    public static boolean isChromeWebStore(@Nullable GURL url) {
+        if (url == null || url.isEmpty()) {
+            return false;
+        }
+        String host = url.getHost();
+        if ("chromewebstore.google.com".equals(host)) {
+            return true;
+        }
+        return "chrome.google.com".equals(host) && url.getPath().startsWith("/webstore");
+    }
+
     /**
      * Check if Request Desktop Site Content Setting is enabled.
      *
